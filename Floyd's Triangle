#include<stdio.h>
int main()
{
    int n,count=0,row,col;
    printf("enter n=");
    scanf("%d",&n);

    for(row=1;row<=n;row++)
    {
        for(col=1;col<=row;col++)
            printf("%d",++count);
            printf("\n");
    }

}
